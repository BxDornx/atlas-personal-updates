# atlas-personal-updates

Update feed for **Atlas Personal** (the single-user Atlas). Served via GitHub Pages at
`https://bxdornx.github.io/atlas-personal-updates/version.json`.

Separate from the multi-user Atlas feed (`atlas-updates`) so the two product lines never
cross-notify. Published by `tools/publish-version.sh` (channel `personal`).
